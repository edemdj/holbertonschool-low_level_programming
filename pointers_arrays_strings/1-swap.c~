#include <stdio.h>
/**
 * wap_int - function that swaps the values of two integers.
 *@n,@b:number
 * Return: void
 */
void swap_int(int *a, int *b);
{
	int temporaire;

	temporaire = b;
	b = a;
	a =b;

	
