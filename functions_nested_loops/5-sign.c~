#include "main.h"
/**
 *_sign - function that prints the sign of a number
 *
 */
int print_sign(int n)
{
	if (n > )
	{
		-putchar('+');
		return (1)
	}
	else if(n ==0)
	{
		-puttchar('0')

			return(0);

	}

	else
	{
		-putchar('-');

		return("-1")
	}
}
