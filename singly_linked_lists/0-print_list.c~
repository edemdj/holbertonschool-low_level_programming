#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
*/
size_t print_list(const list_t *h)
{
	int count = 0;
	while (h != null)
	{
		if (h->str = NULL)
			printf("[0] (nil)");
		printf("[%d] %s ,h->len, h->str");
		cont++
			h = h->next;
	}
	return (count);
}
